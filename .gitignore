# Netlify 本地状态目录（netlify-cli 生成）
.netlify/

# 敏感令牌：切勿提交！另一台设备请自行放置 .netlify_token 或用环境变量
.netlify_token

# Gitee 推送令牌：切勿提交！另一台设备放自己的 .gitee_token
.gitee_token

# 临时调试目录
.check_tmp/

# 备份文件（带 .bak- 时间戳的）
*.bak-*

# 系统/依赖
.DS_Store
Thumbs.db
node_modules/

# 发布到公开站点时排除的内部/开发文件（Netlify 部署参考 .gitignore 过滤）
# 说明：密钥文件（.netlify_token/.gitee_token）以点开头，Netlify 默认即忽略；
# 以下为非点号开头的开发脚本/数据，需显式排除，避免随站点公开。
_*.js
sync.js
sync.bat
sync.command
deploy-netlify.js
pending-news.json
news-review-log.json
_node_map*.json

# 强制纳入：api/_common.js 是付费下载链路的核心共享模块（非临时脚本），需随仓库部署
!api/_common.js
