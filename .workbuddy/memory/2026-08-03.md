# 2026-08-03 工作日志

## 开工前先 pull 的惯例再确认（用户强调）
- 用户在 Windows 那台把 28–31 号新闻 push 到 Gitee，导致 Mac 改动期间分叉冲突（即昨天的 merge 事件根因）。
- 用户明确：在这台 Mac 上做任何相关操作前，**先 git pull 把最新代码 down 下来**。
- 已固化：项目 MEMORY.md 「开工前必做」段落补充了原因与 sync.js 的自动 pull 逻辑；sync.js 在「干净且落后」时自动 `git pull --rebase`，「本地也有改动且落后」时只给 stash→pull→stash pop 顺序、绝不自动动。
- 验证：跑 `node sync.js` 显示落后 0 / 领先 0，Mac 已与 Gitee 一致。

## 收尾动作
- 提交昨天遗留的 `.workbuddy/memory/2026-08-02.md` + 更新后的 `MEMORY.md` 并 push 到 Gitee，避免 Mac 工作树残留未提交改动。

## feature-manifest.html = 改造沟通主文件（用户 2026-08-03 同步）
- 用户明确：这是「功能模块说明文档」，每次网站有新内容更新都整理进此文件；后续改造讨论/需求反馈/改动记录**优先通过此文件交流**。
- 已固化到 MEMORY.md 新增「📄 feature-manifest.html — 改造沟通主文件」段 + 项目技能 SKILL.md 第 1 节加了引用说明。
- 文件当前版本徽章 v1.29 · 2026-07-31，底部更新日志表记录 v1.22~v1.29 改动；改 HTML/CSS 时须在表头追加一行并同步 sw 缓存/README。

## v1.36 素材卡片一键导出（盈利模式讨论落地的免费 MVP）
- 用户探讨盈利模式：痛点是高中生在校难频繁浏览、素材需可带去学校朗读记忆 → 决定先做**免费**「一键导出近 7 天新闻成 A4/PDF 素材卡片」验证需求，付费墙（账号+微信/支付宝）待验证后再做。
- 决策：① 范围选「近 7 天」（合理）；② **否决「仅精选」**——卡片已是浓缩、分析无法再精简，且考试题各异、量要足够多，故全量导出不做过滤。
- 实现：新建独立页 `news-export.html`（读 news-data.js 的 NEWS_DATA，近 7 天全量 105 条），**零依赖**用浏览器原生 `window.print()` + `@media print`（A4/12mm/页脚页码）出 PDF/实体打印；密度预设 紧凑(3列)/标准(2列)/宽松(2列大字)；主站导航加「🖨 导出素材」入口。
- 校验：真实数据+DOM 桩跑渲染 → 105 卡 / 208 解读块 / 区间 2026-07-28~08-03 / `<b>` 富文本正常 / 空数据兜底未误触发。
- 同步：sw.js `shehui-ren-v18`→`v19`（news-export.html 加进 SHELL 预缓存）；feature-manifest 徽章 v1.36 + 更新日志表头加一行；README 版本行/缓存版本/版本历史表同步。
- 已 commit `07f108a` push Gitee（落后0/领先0），并部署上线：`news-export.html` 线上 HTTP 200（草稿+restore 兜底发布，生产部署因 Netlify 免费额度 Forbidden 属预期）。
- 线上地址：https://shehui-ren.com/news-export.html
