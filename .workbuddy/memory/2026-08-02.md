# 2026-08-02

## 站点体检（v1.29 现状盘点）
- git HEAD = `914744e`（deploy 兜底提交），功能版本 v1.29；sw.js `CACHE='shehui-ren-v11'`；feature-manifest 徽章 v1.29 · 2026-07-31。三者一致。
- 节点数 311，`{id:'x', name:...}` 正则扫描**无重复 id**——历史记录的 `rationalization` 重名 bug **已修复**，现拆为 `rationalization_theory`（理论流派，行1156）与 `rationalization_concept`（概念，行1429）。记忆可去掉该待办。
- 连线不是单一 EDGES 数组，分三张表：`EXTRA_CONCEPT_LINKS`(1017) / `CONCEPT_LINKS`(1028) / `THINKER_LINKS`(1089)，运行时在 `edges=[]`(2895/2976) 处组装。写校验脚本别只找 EDGES。
- news-data.js 覆盖 2026-07-25 ~ 07-31 共 7 个日期（符合 S7 上限），**08-01 / 08-02 缺失**（自动化跑在 Windows 机上，Mac 端没有同步）。

## 本次改动
1. **README.md 重写**：v1.23 → v1.29；补 sw 缓存版本、311 节点/三张连线表说明、netlify-cli 安装步骤、部署脚本两段式兜底原理、news-validate.js 桩注入注意事项、v1.23–v1.29 版本历史表。修掉原来 Mac 示例误写 `node.exe` 的错。
2. **deploy-netlify.js 跨平台化**：新增 `detectNode()` / `detectNetlify()`，按 `process.platform` 自动探测 `~/.workbuddy/binaries/node/versions/*/bin/node`（Win 为 `node.exe`）与 workspace 下的 netlify-cli；环境变量 WB_NODE/WB_NETLIFY 仍最高优先级；找不到 cli 时给出可直接复制的 npm install 命令并 exit 2。启动时打印实际使用的 node / cli 路径。已用 `NETLIFY_AUTH_TOKEN=dummy` 干跑验证（安全退出于 NO_NETLIFY_CLI，未触发真实部署）。

## Gitee 连接确认 + 同步机制（用户明确要求）
- 确认结果：**已连 Gitee**，`origin = https://gitee.com/no-works-yet/shehui-ren.git`（fetch/push 同址），`master` 跟踪 `origin/master`，凭据已缓存（fetch 无需交互）。fetch 后**落后 0 / 领先 0**，HEAD `914744e` 与远程一致。
- 用户在公司 Win + 家里 Mac 交替改动，要求**每次开工前**都确认已连 Gitee 且是最新。
- 新增 `sync.js` + `sync.command`(Mac 双击, 已 chmod +x) + `sync.bat`(Win 双击)：检查 git 仓库→origin 是否 Gitee→upstream→fetch→报告落后/领先/未提交。工作区干净且落后时自动 `pull --rebase`；有本地改动则不自动动、提示 stash→pull→stash pop。`--check` 只报告。退出码 0/1/2。已试跑通过。
- 新增项目技能 `.workbuddy/skills/shehui-ren-iterate/SKILL.md`，把「开工前同步 / 版本三处同步 / 部署 403 兜底 / 新闻数据契约 / news-validate 桩注入」整合成一份工作流。
- MEMORY.md 顶部加了开工前必做的醒目条目。

## Mac 端部署环境（已就绪，v1.29 已成功部署验证）
- netlify-cli v27.0.1 darwin-arm64 已装好；`.netlify_token` 已就位（gitignore 已忽略，绝不进 git/部署产物）。
- **第三个 token（nfp_TaCh…）有效**（`GET /api/v1/user` 返回 200）。前两个 token 均 401（含 `GET /sites/{id}` 也 401），确认是凭证本身无效、非类型/本地问题，已指导用户到正确位置重生成。
- **部署成功**：`node deploy-netlify.js` 生产部署返回 `Forbidden`（Netlify 免费额度耗尽，符合预期），自动走「草稿部署 + restore 发布」兜底 → deploy `6a6f6685ff2fae5f1794ca84` state=ready → https://shehui-ren.com；线上首页 curl 验证 HTTP 200。v1.29（含本轮 README/deploy/sync 改动，页面功能仍为 v1.29）线上无误。
- 安全做法（已验证有效）：部署前把 `.netlify_token` 移出仓库目录、用环境变量 `NETLIFY_AUTH_TOKEN` 传给进程，杜绝 token 随发布目录上传；部署后移回。
- 遗留优化（待办）：`netlify deploy --dir=ROOT` 会发布整个仓库目录，`.workbuddy/`、`_*.js` 等内部/调试文件也会被公开托管。下一步加 `.netlifyignore` 排除。
- 本轮改动不涉及 HTML/CSS，sw 缓存仍为 v11，未 bump。

## Mac / Windows 两版 git 合并（整合完成）
- 分叉：Mac 本地有 my commit（README→v1.29 / deploy 跨平台探测 / 独立 sync.js+sync.bat+sync.command / skill / memory）；Windows 在 Mac 改动期间 push 了 `83cdabd`（补 08-01 新闻 + deploy 内置 Gitee sync + 删独立 sync.js/skill/memory）。首次 push 被拒（远程有本地没有的提交）。
- 用户决策：**合并两边**。git merge origin/master（ort 策略，**无冲突**自动合并）。结果：
  - deploy-netlify.js 同时含 Mac 跨平台探测(`detectNode`/`detectNetlify`) 与 Windows Gitee sync(`syncGitee`)。
  - news-data.js 自动合并为 07-26~08-01 共 7 天、每天 15 条（符合 S7，含 08-01）；未混入 07-25。
  - sync.js/sync.bat/sync.command/skill/memory 全保留；.gitignore 含 `.netlify_token` + `.gitee_token`。
- 已 push 到 Gitee（`b1a25e0`，落后0/领先0）。
- 重新部署合并后最新版到线上（含 08-01 新闻）：草稿 `6a6f69c6…` state=ready，HTTP 200。Mac 无 `.gitee_token` → `syncGitee` 自动 warn 跳过（不阻塞）。
- **现状：Mac / Windows / Gitee / 线上 四方一致**（v1.29 + 08-01 新闻 + 双同步机制）。
- 遗留待办：① 加 `.netlifyignore` 排除 `.workbuddy/`、`_*.js` 等，避免内部文件随发布目录公开；② 08-02 新闻仍缺（今天数据未生成）；③ Mac 若要让 deploy 的 Gitee sync 生效，需放 `.gitee_token`（或改用 git credential）。
