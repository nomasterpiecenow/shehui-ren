# 2026-08-06 工作日志

## 拉取 Windows 端大版本（v1.39→v1.48，落后 20 提交已合并）
- 开工前 `node sync.js`：落后远程 20 提交、本地有 5 文件改动（上轮记忆+草案）。按 sync 给的顺序：`git stash -u` → `git pull --rebase` → `git stash pop`，**无冲突**。当前 HEAD = `fe70a84`（v1.48, origin/master）。
- **重大发现：Windows 端已实现完整「速用模式」**，与我们上次讨论的"受控词表"方向一致，但实现路线是"**方案C：AI 语义直写 `essayTopics`**"，而非我提的 lensId→taxonomy 映射。
  - `topic-lib.js`：受控词表 `TOPIC_LIB`（shehui-topics-v1），**9 个母题维度 × ~34 标签**，每标签带 `def` + `keywords` 兜底；**封闭词表**（不可造词，新增须人工评审并 bump 版本）；明确"本阶段不关联社科节点（nodes 暂不用）"。
  - 新闻数据每条新增 `essayTopics:[{id,label}]`（从 TOPIC_LIB 多选 1–4 个，AI 直写）+ `essayQuote`（速用金句 100–130 字议论文语段，必填，`news-validate.js` 硬闸校验）。验证 2026-08-06 数据已落地。
  - 速用模式 UI 已上线：热点页 / 新闻卡片 / 详情面板 / 导出页均渲染 `essayQuote` + 作文主题标签（琥珀色药丸）。feature-manifest v1.39–v1.48 含截图 `contract-shots/*`。
  - 部署前补标脚本 `_annotate_topics.js` 已退役删除；S2.5 主题倾斜配额(Q=3)保留但仅决定名额，`essayTopics` 已全新闻覆盖。
- **我方上轮草案处置**：`topic-taxonomy.js` / `topic-tags.js`（8 领域×35 标签自由词方案）方向被印证但已被官方实现超越，**应归档**（未部署、文件名不同、不冲突）；`news-speed-demo.html`（三风格交叉练手）同理，官方速用模式已更完整，可归档。
- **关键差异/融合点**：我方 taxonomy 8 领域 vs 官方 9 维度，标签高度重合；官方更细（独立"读书学习·自我认知"维度，单列 AI伦理/信息茧房/虚拟真实/知识焦虑/拒绝躺平等）。用户最初最关心的"**标签→新闻反向检索**"：官方数据已具备（`essayTopics` id∈TOPIC_LIB），但**反向检索 UI 页面似乎尚未做**（feature-manifest 只提"帮助用户定位母题"，未见点标签筛新闻的浏览页）——可作为下一步重点。

## 素材分类页（反向检索）落地 · v1.49（预览待验收）
- 决策脉络（对话收敛）：方案 C「可整体收起的完整手风琴（9 大类×34 子标签全展开可点）+ 结果区独占整屏」。分页用**自然下滑无限滚动**（每批 15，筛选展开时仍监听 scroll 但用户通常收起后浏览，不冲突）。**MVP 不含导出**（用户觉得 X=导出全量/Y=逐项开关都不是真 MVP；导出推迟，待真实使用数据再定 Z=导出当前已加载 N 条）。
- 实现：在 `sociology-map.html` 新增第三 Tab「素材分类」(`#page-classify`)，`switchPage` 挂 `ensureClassify()` 钩子；数据聚合遍历 `NEWS_DATA` 统计每个 `essayTopics` 命中数（计数徽标=反向索引）+ 并集(OR)过滤；结果卡片复用速用样式(`essayQuote`+主题芯片)，点卡 `showNewsDetail(id)`（已加跨日期全局回退，分类页卡片也能正确打开）。
- 版本三处同步：sw.js `shehui-ren-v41`→`v42`；feature-manifest 徽章+更新日志表头追加 v1.49 行；README 版本行从过时 v1.36 校正到 v1.49（含缓存版本）。
- 校验：`_check_syntax.js` 内联脚本语法 0 错；`_t.js` 数据验证 105 条新闻、34 主题全部被使用、并集 fairness+youth_duty=48、单主题最大 fairness=46（无限滚动 hold 住，无"200 条强制导出"问题）。
- **部署**：`node deploy-netlify.js --staging` 成功，预览 = `https://6a74a51759bc0a8733e80e59--shehui-ren.netlify.app`（草稿，未转正）。**待用户验收 OK 后再 `node deploy-netlify.js`（不带 --staging）正式上线**。
- 注：本地草案文件（news-speed-demo.html / topic-tags.js / topic-taxonomy.js）仍为 Windows 上线前的旧稿，未删未部署，留待用户定夺归档。
- **未 git 提交**：本轮所有改动（含记忆）均未 commit/push，按规范待用户验收上线时一并 sync 或另行提交。

## 素材分类筛选器视觉打磨（验收反馈迭代）
- 用户预览后反馈筛选器"丑陋"，尤其收起后"已选 N：文本+杂排芯片"换行参差、不对齐。重做：① "已选"回显改为**等高统一芯片（红色软底+圆形 × 关闭钮）+ 计数徽标（ct-count）+ 「清除全部」按钮**（flex 等高、换行整齐）；② 分组标题加**红色强调条**（ct-gh::before）；③ 开关按钮加**渐变底 + 悬停红边/阴影 + 箭头翻转**（cf-caret 随 .open 旋转 180°）；④ 芯片悬停上浮+红边、计数徽标独立药丸、选中态带阴影。HTML 开关拆出 `.cf-label`+`.cf-caret`（JS 改 set `.cf-label` 文本而非整体 textContent），tray 重构成 ct-count/ct-x/ct-clear 结构并加 clear 监听。
- 校验：内联脚本语法 0 错。sw.js `v42→v43`（内联 style 改需 bump 缓存）。feature-manifest v1.49 行追加打磨说明。
- **重新出预览**：`https://6a74a861602d4f2eb68d0582--shehui-ren.netlify.app`（草稿覆盖）。仍待用户最终验收 OK 后再正式发布。

## 素材分类页 · 验收反馈二轮修复（Tab 顺序 + 顶部间距）
- 用户反馈两点：① 导航 Tab 顺序——素材分类应在「知识图谱」之前（第二位）；② 手机端素材分类页顶部被吸进顶栏（"03 素材分类"几乎贴顶，而社会热点页"02 社会热点"有距离）。
- 修复：① 导航改序 `社会热点 → 素材分类 → 知识图谱`（line 888-890）。② 根因：全局 `.section{padding:36px 40px}` 顶部内边距，被 `#page-classify .section{padding:0 24px}` 以 ID 特异性压成 0；移动端同理（媒体 `.section{padding:28px 18px}` 被 `#page-classify .section` 覆盖）。补回：`#page-classify .section` 桌面 `padding:36px 24px`、移动媒体 `padding:28px 16px`，与社会热点页一致。编号"03/素材分类"保持（与社会热点"02"同为装饰性、不对应 Tab 位序）。
- sw.js `v43→v44`。feature-manifest v1.49 行追加 b)/c) 说明。
- **重新出预览**：`https://6a74a9d32a71cbf258e97dd3--shehui-ren.netlify.app`（草稿覆盖）。仍待用户最终验收 OK 后再正式发布。
- 用户发现 Tab 大编号错位：「社会热点」显示 02（应为 01）、「素材分类」显示 03（应为 02）。开工前先 `node sync.js`（落后远程 0、工作区干净）。修复：① 社会热点 `02/社会热点`→`01/社会热点`；② 素材分类 `03/素材分类`→`02/素材分类`；③ 知识图谱页本无 section-num 不动。sw.js `v44→v45`。feature-manifest v1.49 行追加 d) 说明。预览 `https://6a74aac312bae7c801146067--shehui-ren.netlify.app`。
- **用户验收发现素材分类页新闻卡片点不开**。根因：详情面板 `#newsPanel` 原位于 `#page-topics` 内部（line ~1002），切到「素材分类」Tab 时父级 `.page` 变 `display:none`，连带把 `position:fixed` 的面板一起隐藏，点击虽执行 `openClassifyNews→showNewsDetail` 但面板不可见。修复：把 `#newsPanel` 与 `#dateBackdrop` 移出 `#page-topics` 成为全局元素（置于所有 `.page` 之外），分类页卡片点击即可正常滑出详情面板。showNewsDetail 跨日期回退逻辑不变（5201-5209）。`dateBackdrop` 仅服务移动端日期抽屉，移出无害。sw.js `v45→v46`。feature-manifest v1.49 行追加 e) 说明。预览 `https://6a74ac31bdd728aa4252ca66--shehui-ren.netlify.app`。仍待用户最终验收 OK 后再正式发布（本轮含编号修正+卡片点击修复全部改动尚未 git commit/push）。
- ✅ **用户验收通过，已正式发布**。git commit（7 文件）+ push origin master（`fe70a84..a71eadc`）；生产部署走「草稿+发布」兜底成功 → **https://shehui-ren.com**（shehui-ren-v46）。3 个旧草案（news-speed-demo.html/topic-tags.js/topic-taxonomy.js）仍**未跟踪、未提交、未删**，待用户决定归档。
