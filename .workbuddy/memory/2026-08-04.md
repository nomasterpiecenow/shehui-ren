# 2026-08-04 工作日志

## 产品定位讨论 + 速用版练手样例
- 与用户讨论盈利/定位：高中语文老师调研反馈（素材高深、联想不到作文素材；中下层学生"写不出作文"而非"写不好"）。
- 定 A 方向：首页 + 导出卡片加「速用 / 深度」切换，**默认速用**。速用 = 摘要(`gist`) + 一句套用句；深度 = 全套多视角解读。
- 练手：建 `news-speed-demo.html`，用最新一篇真实新闻（2026-08-03 最高检 12–14 岁追诉标准，头条）演示**单篇速用版样式 + 切换**。套用句为手写示例文案（正式版由 `usable` 字段提供）。仅本地预览，**未部署、未改主站、未 bump 版本**。
- 数据缺口已记入 MEMORY.md：需新增 `usable` 字段（采编端 Windows 生成入库）。
- 套用句应试化迭代：用户要求偏应试、"假大空"金句风（学生爱写、阅卷吃香）。联网调研高考满分作文套路（对仗/四字格/宏大意象/回扣材料升华）后，把 demo 套用句改为应试金句调性，并记入 MEMORY 速用模式规范。提供了多版备选供用户挑调性。
- **三风格交叉（2026-08-04 续）**：用户指出排比三连/对称标题句/青年呼告三者不互斥、应**按素材特性交叉选**（一页全排比只能当结尾，不合适）。重做 demo 为三篇真实新闻交叉演示：法治→对称句、经济→排比三连、社会互助→青年呼告，每篇标注"为何用此风格"。交叉原则已固化进 MEMORY.md 速用模式段（采编"选择器"：法治用对称/经济用排比/社会用呼告）。demo 仍仅本地预览、未部署。注：本轮 Bash 工具临时不可用，改用 Read/Edit 推进，sync 沿用上轮结果（仅改本地 demo 不部署，风险低）。
- **「适用话题」标签逻辑（对标小红书，路线 A）**：用户发小红书高赞作文素材帖，指出我们缺"适用主题"标签。定路线 A（基于 lensId 自动映射，非人工打标）。建 `topic-tags.js`（草案，未部署）：三层——EXACT 精确词表 + FALLBACK 学科兜底 + 聚合去重(≤5)。验证 105 条全有标签、均值 4.96、0 缺失。待用户确认话题词质量后集成进速用模式。决策：暂不做"手写笔记风"视觉，先攻底层功能逻辑。
- **受控主题词表（taxonomy）提案（架构升级）**：用户指出自由生成标签（"社会观察/人间万象"）太泛、无指向、不可反向检索/不可拓展，要求先调研全网高考/联考/人民日报时评抽象出可复用标签体系。已搜：高考12大类、2026联考预测Top4、人民日报9大主题时评、人民日报评论部《时评写作解析》9类、高考18题型等。抽象出 `topic-taxonomy.js` 提案：8 大领域（青春·成长/家国·时代/文化·传承/科技·人文/社会·公义/品德·修身/自然·生态/思辨·哲理）× 35 个受控标签，每标签对应一类作文命题方向、可反向检索。下一步：用户审阅领域/标签划分 → 做 lensId→受控标签映射 + 标签反查新闻原型。原 topic-tags.js 自由词方案弃用。
