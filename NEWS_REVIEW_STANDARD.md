# C2 新闻审核标准（NEWS_REVIEW_STANDARD）

> 本文件是「社会人」网站新闻数据（C2）**全自动更新**的权威审核标准。
> 自动化任务（每日 9:06 / 21:06 触发）作为**抓取者 + 审核者**一体，必须严格按本标准筛选、核实、入库，并产出可复核日志。
> 任何一条新闻进入 `news-data.js` 前，都必须逐条通过以下 S0–S8 检查。

---

## 一、数据 Schema（每条新闻）

既有字段（前端已依赖，不可改名）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | number | 当日批内唯一序号 1–15 |
| `title` | string | 新闻标题 |
| `source` | string | 来源，如 `微博热搜` / `综合新闻` |
| `url` | string | **原文直链**（必须可访问，HTTP 200） |
| `kicker` | string | 可选小标签，如 `职场 · 社会学 / 心理学` |
| `gist` | string | **一句话立论**：开篇点明该新闻的社会学内核（替代旧版背景段） |
| `thread` | `[{t, x}]` | **事件脉络**时间线：把背景-发展-结果串成有序步骤 |
| `interpretations` | `[{q, naive, lens, lensId, body, aha, research?}]` | **疑问驱动解读**：每条 = 读者真问题 `q` → 常识误判 `naive` → 用 `lens`（理论名，`lensId` 为节点 id）作"解答工具" → 解读 `body`（可含 `<b>理论名</b>`）→ 顿悟收尾 `aha`；概念降为尾部小标签而非压顶大 title |
| `theories` | `[{id, label}]` | 关联的知识图谱节点（id 必须 ∈ 图谱节点集），用于"关联知识"与子图 |

> 旧结构 `background`/`event`/`outcome`/`perspectives[{theory,plain,detail}]` 仍被前端兼容渲染（历史日期新闻），但**新入库批次统一用上表新结构**。

新增可选字段（用于追溯与校验，前端忽略即可）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `major` | boolean | 是否特大新闻 |
| `platforms` | string[] | 判定为特大所依据的出现平台（≥4 个） |
| `review` | `{strength, disciplines}` | 审核结论：最强关联强度、覆盖学科 |
| `interpretations[].research` | `{title, authors, year, venue, url, summary, fromNode}` | **前沿研究（可选，嵌套于解读，best-effort）**：当某社科视角有直接相关的前沿研究时，把研究挂到该 `interpretation` 上，使解读直接用研究；`fromNode` 为研究"发展自"的上游知识节点 id（必须 ∈ 图谱节点集）。不再有新闻顶层 `research[]`。 |
| `essayTopics` | `[{id, label}]` | **作文主题标签（可选，速用模式用）**：id 必须 ∈ `TOPIC_LIB.topics`、label 等于其 label。由两路写入——① S2.5 主题倾斜通道新闻在抓取时由审核者直接写入（dim ∈ 青春/家国/思辨三维度）；② 其余新闻由部署前幂等补标脚本（`_annotate_topics.js`）按"社科节点映射 + 关键词"写入。该脚本遇到已有 `essayTopics` 的新闻会跳过，故两路不冲突、不重复。 |

---

## 二、审核标准（S0–S8）

### S0 合规硬闸门（最高优先级）
- **政治敏感、谣言、NSFW、血腥暴力内容一律剔除**，优先级高于条数、交叉、强度等所有目标。
- 来源须为可核验媒体；纯社媒传言且无第二信源者剔除。
- **来源可信度分层（2026-07-23 新增）**：
  - **一级（可直接用）**：主流媒体 / 政府 / 机构 / 正规新闻站（含其官方聚合镜像，如光明网转大河报——`source` 写原始出处如「大河报」而非聚合站）。
  - **二级（热议类话题）**：若以「用户讨论 / 热议」为新闻角度，必须另有 **≥1 条一级编辑源**报道过该话题；`source` / `url` 须指向该编辑源文章页，原始 UGC 帖仅作「讨论素材」，不得作为主源。
  - **排除**：微头条 / 抖音个人 / 小红书个人 / 知乎回答 / 微博个人 / 营销号 等 **UGC 个人帖**，除非有编辑源佐证且 `source` 明确标注为「用户讨论」而非冒充媒体。
- 命中即丢弃，不进入后续任何环节。

### S1 条数精确 = 15
- 每个日期键恰好 **15 条**，不多不少。
- 批次内**去重**：15 条不能是同一事件的不同表述。
- 特大超额时：按平台数取 top15；若特大在 1–14 条之间，全部纳入后再补足非特大至 15。

### S2 特大必含（跨平台热榜交叉）
- 抓取 **≥4 个热榜指数**（如微博热搜 / 百度热搜 / 知乎热榜 / 头条 / 抖音），将候选按事件归并。
- 同一事件出现在 **≥4 个**指数即判为**特大**；记录 `major:true` + `platforms:[...]`。
- 特大须有 **≥1 个有效理论链接**，但**不强制跨学科**。
- 当日若有特大，必须涵盖于 15 条之内。

### S2.5 主题倾斜配额（青春 / 家国 / 思辨 · 官媒正向通道）
> 目的：当前候选池完全来自热榜，而「青春奋斗 / 家国情怀 / 时代使命 / 思辨辩证」类作文母题新闻几乎不上热榜，导致速用模式下的作文主题标签（essayTopics）在这三类上长期稀疏。本规则在保持 S0 / S2 / S4 / S6 底线不变的前提下，为这类新闻**专门预留名额 + 降低热度门槛 + 增设官媒定向抓取源**。

- **配额 Q = 3**：每批次从 15 条中预留 **3 条**专给本通道；其余由热榜新闻填满（组批逻辑见 S7）。
- **准入四闸门（须全部满足）**：
  - **G1 来源白名单**：`source` ∈ 官媒清单（新华社 / 人民日报 / 央视新闻 / 中国青年报 / 共青团中央 / 人民网 / 光明网 / 央广网 / 教育部及各省市级党报官网）。聚合镜像须写原始出处；UGC 个人帖一律排除（见 S0）。
  - **G2 主题信号**：内容明确命中**青春成长·青年担当 / 家国情怀·时代使命 / 思辨辩证**三维度之一（即 `TOPIC_LIB` 中 dim 为这三者之一的标签），由关键词 + LLM 判读确定，并须写出对应 `essayTopics`。**本闸门是核心——「降门槛」只惠及作文母题，不惠及泛化的官媒正能量。**
  - **G3 正向框架**：属建设性 / 鼓舞性叙事，无事故 / 腐败 / 批评性负面事件框架；排除「某青年涉案」「某地问题被通报」之类虽有青年 / 家国字样但非正向的报道。
  - **G4 低门槛准入**：热榜出现 **≥1** 个指数（或仅官方首发即算候选），而非 S2 的 ≥4；但仍须通过 S0 / S4(≥1 strong 链接) / S6 底线。
- **定向抓取源（步骤 1 / 步骤 3 新增）**：除热榜外，额外用 WebSearch 定向检索官媒的青春 / 家国 / 思辨主题报道（如「青春 奋斗 新华社」「家国情怀 时代使命 人民日报」「青年 担当 中国青年报」），结果并入候选池、标注来源类型为编辑源（官媒）。
- **essayTopics 由抓取时直接写入**：本通道新闻在入库时即由审核者写 `essayTopics:[{id,label}]`（id 必须 ∈ `TOPIC_LIB.topics`），后续部署前幂等补标脚本遇到已有 `essayTopics` 会跳过，不重复、不破坏。
- **组批顺序（S1 / 步骤 5）**：含全部特大 → 热榜非特大补足至 (15 − Q) → 主题配额 Q 条填满 15。**仅当 (15 − 特大数) ≥ Q 时配额生效**；若特大过多导致 (15 − 特大数) < Q，则配额按比例缩减（优先保特大），多余配额不强行凑数。
- **日志（S8）**：`chosen[]` 中本通道新闻须标记 `themePick:true` 与命中的 `essayTopics`，便于人工复核配额是否被滥用。

### S3 学科交叉（整体覆盖 + 单条尽量）
- 15 条**整体**须触及 **社会学(soc) / 心理学(psy) / 经济学(econ)** 三科。
- 单条非特大新闻**尽量**关联 ≥2 个学科。
- 若与 S4 冲突（强行跨科会导致弱链接），**以 S4 强度为准**：宁可不跨科，也不要弱关联。

### S4 关联强度反向核实
三级判定（基于「新闻正文 / 核心议题」与「知识节点定义」的重合度）：
- **strong（核心主题）**：该知识点是新闻的主线/核心议题。
- **medium（显著角度）**：是新闻的一个明确重要角度。
- **weak（仅分支/时间巧合/名词蹭热度）**：仅一个小分支、某个时间节点、或名字偶然出现——**视为过弱，丢弃该链接**。
- 每条新闻须有 **≥1 个 strong 链接**；weak-only 的链接必须剔除。
- 每条 2–3 个链接（含 ≥1 strong）。
- `theories[].id` **必须 ∈ 图谱节点集**（硬性校验，见 S6）。

### S5 时效与新鲜度
- 9:06 批次：取近 **24 小时**新闻。
- 21:06 批次：取当日约 **12:00 之后**新发新闻（避免重复抓取上午已发的）。
- **跨日期去重**：近 7 天内已覆盖的同一事件不重复；特大事件持续发酵可续发，但**连续 ≤3 天**。

### S6 字段与链接完整性
- 新结构必填非空：`id` / `title` / `source` / `url` / `gist` / `thread`(≥1 步) / `interpretations`(≥1 条，且每条约含 `q` `naive` `lens` `lensId` `body` `aha`)；旧结构必填 `background`/`event`/`outcome`/`perspectives`（仅历史批次）。
- `url` 必须存在且为 `http(s)` 链接，最好实测可访问（HTTP 200）。
- `theories[].id` 必须 ∈ 图谱节点集；`label` 必须等于该节点名。
- `interpretations[].lensId` 必须 ∈ 图谱节点集（与 `theories` 之一对应）。
- `interpretations[].body` 须含 `<b>理论名</b>` 高亮，且 `aha` 须以"顿悟"收尾。
- **标题 / 立论事实一致性（2026-07-23 新增，入库前硬闸）**：重读主源，核对 `title` 与 `gist` 中的关键事实（人物 / 事件 / 数字 / 考试类型 / 路径 / 地点 / 时间）与源文一致；引用专业路径 / 制度（如「中本贯通 = 中考路径」「春季高考 ≠ 普通高考」）时标题不得自相矛盾——出现「高考 + 中本贯通」此类冲突直接打回修正或丢弃。

### S7 留存与回退
- `news-data.js` 中**仅保留最近 7 个日期**，超出则丢弃最旧日期键。
- 过滤后可用候选 **< 15 且 ≥ 8**：降级发布（放宽 S3 交叉、允许 medium 链接），但仍遵守 S0/S4/S6 底线。
- 过滤后 **< 8 条**：发布可用部分并**告警**，不强行凑数、不降合规底线。

### S8 可复核日志
- 每批产出 `news-review-log.json`，含：
  - `chosen[]`：入选的 15 条及入选理由（是否特大、平台、学科、最强强度）。
  - `rejected[]`：被淘汰候选及淘汰原因（S0 合规 / 弱链接 / 重复 / 失效id / 过期 / 不足）。
  - `summary`：批次日期、候选总数、特大数、三科覆盖、校验结果。
- 目的：无人审核时供人工抽查信任度。

### S9 前沿研究（可选，嵌套于视角，best-effort，每日自动检索挂载）
- **目的**：仅用有限的经典知识节点解读每日新闻会"落后于现实"。对每条新闻，结合国内外**真实前沿研究**让解读更贴前沿；前沿研究不是独立板块，而**内嵌于「社科视角解读」的对应视角**——若某视角有直接相关的研究，该视角的 `detail` 就应直接用这项研究，并附该研究的**上游（更早）知识节点**（`fromNode`）作为溯源与知识解读。
- **发现方式**：对每条入选新闻，基于标题/主题 + 已关联 `theories`，用 WebSearch 做**中、英文各一次**检索（如「主题 研究 / empirical study / 调查报告 / systematic review」）。从结果抽取**最相关且可核验**的真实研究（0–1 条/解读，整条新闻 0–2 条），挂到最匹配的那个 `interpretation` 上。
- **映射 `fromNode`（上游节点）**：每条研究须映射到其**真实上游知识节点**（优先取该研究发展自的图谱节点，或话题邻近节点）；`fromNode` 必须 ∈ 图谱节点集。该节点会在视角内以「溯源 → 标签」按钮呈现，并附一句该节点的知识解读。
- **校验闸门（不达标即剔除，不降级、不强行补）**：
  1. **与对应视角 / 新闻直接相关，否则剔除**（核心闸门：仅名词撞车、实质无关的研究一律不要）；
  2. 无真实 `url`（非 `http(s)`）→ 剔除；
  3. 来源为博客 / 营销号 / 自媒体 / 聚合页（无明确研究主体）→ 剔除；
  4. 无法映射到任一上游节点 → 剔除；
  5. 命中 S0 敏感（政治敏感 / 谣言 / NSFW）→ 剔除。
- **best-effort 不阻塞**：无任何达标研究时，该新闻照常发布（不写 `research`）；前沿研究是**加分项而非必选项**，绝不为凑数引入弱/假研究。
- **国际研究**：可纳入有分析价值的国外研究，但同样受 S0 合规约束。
- **写入字段**：`interpretations: [{ q, naive, lens, lensId, body, aha, research: { title, authors, year, venue, url, summary, fromNode } }]`；`summary` 用 1–2 句中文写给普通读者，`url` 指向论文页/预印本/机构报告正文；研究的 `body` 须直接引用该文献。

---

## 三、自动化执行流程（抓取 + 审核一体）

1. **抓取**：WebFetch/WebSearch 拉取 ≥4 个热榜指数 + 综合新闻，形成约 40–60 条候选池；并为每条候选标注**来源类型**（编辑源 / UGC 个人帖 / 热议话题），UGC 起源的热议话题须额外检索一条编辑源佐证，否则不纳入候选池。
   - **官媒主题定向抓取（S2.5 通道）**：额外用 WebSearch 定向检索官媒发布的「青春 / 家国 / 思辨」主题报道，建议查询形如「青春 奋斗 新华社」「青年 担当 中国青年报」「家国情怀 时代使命 人民日报」「思辨 辩证 央视」等，取近 24h（早班）/ 当日 12:00 后（晚班）的结果，并入候选池并标注来源类型为**编辑源（官媒）**；这些候选不要求上热榜，按 S2.5 的 G1–G4 评估，与热榜候选一同进入后续打分与组批。
2. **S0 合规过滤**：剔除敏感/谣言/NSFW。
3. **S2 特大识别**：跨热榜归并事件，统计平台数，标 `major` + `platforms`。
4. **候选打分**：对每条判定 S4 强度（读正文 vs 节点定义）、S3 学科标签、S6 链接合法性（仅用真实节点 id）。
5. **S1 组批（含 S2.5 主题配额）**：① 包含全部特大；② 补足热榜非特大至 **(15 − Q)** 条（Q=3，见 S2.5）；③ 若可用主题候选（满足 G1–G4）≥1，从中取最贴合作文母题的 **Q 条**填满至 15，并在这些新闻上写 `essayTopics`（id 必须 ∈ TOPIC_LIB、dim ∈ {青春成长·青年担当, 家国情怀·时代使命, 思辨辩证}），`chosen[]` 标记 `themePick:true`；批次内去重（不可同一事件多表述）；S5 时效/跨日期去重。**仅当 (15 − 特大数) ≥ Q 时配额生效**，否则配额按比例缩减（优先保特大），不强行凑数。
6. **S6 校验前置**：字段齐全、`url` 可访问、`id` 合法；并**重抓主源核对 `title` / `gist` 关键事实与源文一致**，领域自洽冲突（如「高考 + 中本贯通」）直接打回修正或丢弃。
7. **S9 前沿研究发现**：对每条入选新闻，基于标题/主题 + 已关联 `theories` 做中、英文各一次检索，抽取最相关且可核验的真实研究，挂到最匹配的 `interpretation` 上（写入该解读的 `research`，并设 `fromNode` 为真实上游节点）；研究须与对应解读/新闻直接相关，否则剔除；无达标则整条新闻不写研究（best-effort，不阻塞）。
8. **入库**：将 15 条写入 `news-data.js`（新增当日日期键 + 按 S7 修剪旧键）。**本批经 S2.5 选入的主题倾斜新闻（themePick）须内联 `essayTopics:[{id,label}]`，id 必须 ∈ TOPIC_LIB.topics、label 等于其 label；其它新闻不写 essayTopics（由部署前幂等补标脚本统一处理）。**
9. **S8 写日志**：输出 `news-review-log.json`。
10. **兜底校验**：运行 `node news-validate.js`；不通过则保留日志并告警，**不发布脏数据**。

---

## 四、边缘情况与判定优先级

| 优先级 | 规则 |
|---|---|
| 1（最高） | S0 合规闸门：宁可少发，绝不触碰敏感/谣言/NSFW |
| 1.5 | S0 来源可信度分层：UGC 个人帖排除；热议话题须有 ≥1 编辑源佐证，主源指向编辑源 |
| 2 | S6 硬性：`id` 合法、`url` 有效、字段完整 |
| 2.5 | S6 标题/立论事实一致性：与源文冲突或领域自洽矛盾（如高考+中本贯通）直接打回 |
| 3 | S4 强度：`weak-only` 链接必剔 |
| 4 | S1 条数精确 + 批次内去重 |
| 5 | S2 特大必含 |
| 5.5 | S2.5 主题倾斜配额：Q=3，仅当 (15−特大数)≥Q 生效；G1–G4 须全满足，themePick 须写 essayTopics |
| 6（软目标） | S3 学科交叉：与 S4 冲突时让位于强度 |
| 7 | S5 时效/去重、S7 留存/回退 |
| 8 | S9 前沿研究：best-effort 加分项，嵌套于最匹配解读(interpretation)；须与对应解读/新闻直接相关，否则剔除，绝不引入弱/假研究 |

> 当「学科交叉(S3)」与「关联强度(S4)」冲突时，**强度优先**：不可为凑交叉而保留 weak 链接。
